---
description: Plan a bug fix or feature by investigating the codebase first
---
Use the `planner` subagent to investigate and produce an implementation plan for: $@

Save the plan to `.omp/plans/<slug>.md` and tell me the slug when done.
